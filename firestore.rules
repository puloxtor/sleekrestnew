rules_version = '2';

service cloud.firestore {
  match /databases/sleekrest-db/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
  
  match /databases/(default)/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
